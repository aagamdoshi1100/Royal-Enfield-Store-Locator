import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Bike Dealers Near You | Royal Enfield Motorcycle Dealers In India",
  description:
    "Find an authentic Royal Enfield motorcycle dealer near you online in India. Search your nearby bike dealers on our website using country, state & city filter",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
